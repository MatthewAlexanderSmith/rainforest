<h1>Products (<%= @products.count %>)</h1>

<%= link_to "New Product", new_product_path %>

<ul>
  <% @products.each do |p|%>

      <li><%= link_to p.name, product_path(p.id)%>, <%= p.description %> - <%= p.price_in_cents %></li>

  <% end %>
</ul>